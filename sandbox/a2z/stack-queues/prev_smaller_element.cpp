#include<bits/stdc++.h>
using namespace std;

vector<int> nse(vector<int> arr) {
    int n = arr.size();
    int ans[n];
    stack<int> st;

    for(int i = 0;i<n;i++) {

        while(!st.empty() && st.top() > arr[i]) st.pop();
    }
}


vector<int> nse (vector<int> arr ) {
    int n = arr.size(); 
    itn ans []
}


int main() {

    return 0;
}

// chutkapyasa