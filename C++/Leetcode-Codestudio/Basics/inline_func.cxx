#include<bits/stdc++.h>
using namespace std;

inline int getMax(int &a,int &b) {
    // return max(a,b);
    return 
}

int main(){

int a {3} , b{5};

int max = getMax (a,b);
cout<<"max is =  " << max;



return 0;
}